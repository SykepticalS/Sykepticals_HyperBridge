# 锁屏小组件（负一屏）

本文记录 HyperIsland 在锁屏负一屏实现「系统小组件」功能的方案与实现细节，面向后续维护。

## 1. 目标

- 锁屏向右滑进入小组件页；手指滑动多少，锁屏内容就同步下沉、模糊多少，完全进入后展示小组件页。
- 支持添加、删除、长按进入编辑、拖拽排序等基本操作。
- 通过配置在「设备互联（融合设备中心）」与「小组件」两种负一屏内容之间切换。
- 过渡必须顺滑，不能有跳变。

## 2. 关键结论

负一屏的横向滑动动画、锁屏下沉与模糊**由系统既有手势引擎完成**。小组件页面只仲裁横向翻页、纵向滚动与长按拖动，不自行绘制页面进出动画。

系统链路（HyperOS 3 / SystemUI）：

- 手势入口：`KeyguardPanelViewInjector.onTouchEvent(...)` → `KeyguardMoveHelper`。
- 平移与模糊：`KeyguardMoveHelper.setTranslation(f, ...)` 会
  - 平移 `MiuiKeyguardMoveLeftViewContainer`（`mLeftView`）；
  - 调用 `updateKeyguardInfoBlurRatio(...)` 让锁屏前景层模糊/变暗；
  - 缩放并淡出锁屏组件（时钟、信息层等）。
- 因此只要把小组件页作为 `MiuiKeyguardMoveLeftViewContainer` 的子 View，页面就会随手指逐帧平移，锁屏内容同步下沉模糊，天然无跳变。

## 3. 为什么页面必须放在 SystemUI 进程

创建 `AppWidgetHost` 需要 `android.permission.BIND_APPWIDGET`，普通 App 进程拿不到；而 `com.android.systemui` 的 Manifest 已声明该权限。因此小组件页直接构建在 SystemUI 进程内，不走远程 Activity / Overlay 传输。

（对比：设备中心走的是远程 Activity + 远端 leash 框架，见 `LockscreenActivityPage*`。小组件不能复用该框架。）

## 4. 文件与职责

| 文件 | 职责 |
| --- | --- |
| `xposed/hook/SystemUI/LockscreenWidgetPageView.kt` | 小组件页面 View：`AppWidgetHost`、增删/编辑/拖拽排序、选择器、持久化 |
| `xposed/hook/SystemUI/LockscreenWidgetPageHook.kt` | 把页面注入负一屏容器，并强制走本地平移分支 |
| `xposed/hook/SystemUI/LockscreenNegativePageHook.kt` | 组合根：按模式二选一安装「设备中心」或「小组件」Hook |
| `xposed/hook/SystemUI/LockscreenNegativePageMode.kt`（同文件内对象） | 模式常量 |

## 5. 实现细节

### 5.1 页面注入

`LockscreenWidgetPageHook` 挂载点：

- Hook `MiuiKeyguardMoveLeftViewContainer.onAttachedToWindow`，在容器加入视图树后注入页面。
- Hook `MiuiKeyguardMoveLeftViewContainer.inflateLeftView`，MiUI 重新 inflate 杂志/控制中心内容后重新置顶页面。

注入时：

- 将容器内非小组件子 View 设为 `GONE`（隐藏原生杂志/控制中心内容），保留容器本身（容器负责承载系统平移与背景模糊）。
- 以 `MATCH_PARENT` 添加 `LockscreenWidgetPageView`，打上 `PAGE_TAG` 便于查找与去重。

### 5.2 强制本地平移分支

系统根据 `KeyguardMoveLeftController` 的几个开关选择不同分支，小组件模式需要组合成「本地页面、纯平移」：

| 方法 | 覆盖值 | 原因 |
| --- | --- | --- |
| `supportMoveToRight()` | `true` | 启用负一屏 |
| `isLeftViewLaunchActivity()` | `true` | 走本地平移分支，而不是远程 `LockScreenMagazineClient` Overlay |
| `isSupportSwipeToLaunchMagazine()` | `false` | 不拉起远程杂志页；控制器 `onTouchMove` 返回 false，`KeyguardPanelViewInjector` 直接调用 `setTranslation` |

同时拦截 `LockScreenMagazineController.startMagazineLeftActivity()`，避免松手结算时误拉起杂志页（因为 `isLeftViewLaunchActivity()` 为 true 时结算动画可能请求启动它）。

以上覆盖仅在小组件模式生效（`isWidgetMode()` 判断）。

### 5.3 小组件增删改

`LockscreenWidgetPageView`：

- 宿主：`AppWidgetHost(context, HOST_ID)`，`onAttachedToWindow` 时 `startListening()`，`onDetachedFromWindow` 时 `stopListening()`。
- 添加：`allocateAppWidgetId()` → `AppWidgetManager.bindAppWidgetIdIfAllowed(id, provider)` → `createView(...)`；若 provider 声明了 `configure`，发送 `ACTION_APPWIDGET_CONFIGURE` 拉起配置页。
- 删除：`deleteAppWidgetId(id)` 并从列表移除。
- 长按由卡片父容器的 `dispatchTouchEvent` 统一识别，避免被 `RemoteViews` 吞掉；长按触发时取消 HostView 的原触摸序列，父卡片持续接管后续 `MOVE/UP` 事件，不显示独立编辑模式。
- 拖动中的小组件使用独立的视觉位移跟手，排序目标变化时相邻卡片实时平移动画避让；拖动目标不会从父容器移除，避免触摸流被取消。松手后才提交顺序并恢复统一卡片布局。
- 尺寸按官方四列网格换算：先根据 `minWidth/minHeight` 反推常见的 `1x1`、`2x2`、`2x4` 等 span，再以统一网格单元尺寸计算卡片宽高，纵向最多四行，不能直接把 provider 的 `minHeight` 当作最终高度。
- 当前对比阶段暂时禁用模块外层卡片背景，仅保留 `AppWidgetHostView` 的 provider 背景；内层统一做圆角裁切，便于确认系统背景是否已经足够。
- 持久化：顺序写入 `SharedPreferences("hyperisland_lockscreen_widgets")` 的 `widget_ids`（逗号分隔）。加载时清理已卸载 provider 的失效 id。

### 5.4 选择器

右上角 `+` 弹出覆盖层，列出 `AppWidgetManager.installedProviders`，显示 provider 图标与名称；搜索框主动请求焦点并显示 IME，点击 provider 即绑定添加。

## 6. 配置切换

- 业务 key：`pref_lockscreen_negative_page_mode`，取值 `device_center` / `widgets`。
- 已登记 `ConfigManager.CORE_PREF_KEYS` 与 `XposedPrefsSyncApp.CORE_PREF_KEYS`，保证同步到 Hook 进程。
- 设置页：`SystemUiHookPage` 在「锁屏设备中心」开关下新增「负一屏内容」下拉框。
- 模式选择在进程加载时决定安装哪套 Hook；切换后需重启 SystemUI 生效。

`LockscreenWidgetPageHook.FORCE_WIDGETS = false`，按设置项选择小组件或设备中心。

## 7. 与设备中心的互斥

设备中心 Hook 会隐藏 `mLeftView`（小组件页所在的容器），两者不能同时安装。`LockscreenNegativePageHook` 按模式二选一：

- 小组件模式：只装 `LockscreenWidgetPageHook`，不装 `LockscreenActivityPageHook` / `LockscreenDeviceCenterHook`。
- 设备中心模式：只装设备中心相关 Hook。

`HyperIslandModule` 在小组件模式下也会初始化负一屏 Hook（不再依赖 `pref_lockscreen_device_center` 开关）。

## 8. 注意事项

- 页面自身不处理横向手势平移，`KeyguardMoveLeftController.onTouchMove` 在小组件模式返回 `false`，由 `KeyguardPanelViewInjector` 统一驱动双向平移、渐变和模糊。
- 长按拖拽只在卡片内部接管触摸，未触发长按时不拦截事件，避免影响负一屏横向退出。
- 页面根节点在滚动区域按下时先保留事件流，再根据位移和速度判断方向；纵向滑动取消小组件内部触摸并交给 `ScrollView`，横向滑动释放给 SystemUI，静止长按后才允许拖拽卡片。
- `mLeftView` 被系统 `setVisibility` 控制，页面仅作为其子 View，不做可见性干预。
